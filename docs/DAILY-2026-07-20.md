# 工作日报（2026-07-20）

> 分支 **`test-from-4841de6a`**，当日 HEAD **`8a262ff`**（pick_prefilter 接入 + 碰撞帧调试日志 + docker cp 联调）。  
> **镜像 tag 未变**：`20260629-test-from-4841de6a-ce4fe0a`；下午以 **`docker cp` + restart** 热更新 event-worker/UI，未重打镜像。  
> 标定来源：**`visual-dps-data-collector`** `try/speed-filter`（commit **`3793f19`**）。

---

## 一、代码提交（已入库）

### 1. pick_prefilter 模块与 Event Worker 接入（`81307a7`）

**门控规则**（与离线标定一致）：

```text
block = ankle_max_speed_norm > 0.081770
    AND NOT triple90
    AND is_standing(shoulder_hip_knee_angle_min >= 140)
```

- 新增 `services/event_engine/pick_prefilter/`（config / features / angles / gate / service）
- `collision.py` Protocol 钩子；`worker.py` 按 `collision_prefilter.enabled` 注入与 reset
- `runtime_config_service.py`：`collision_prefilter.*` + 设置页「碰撞前置门控」UI
- 单测 8 项 + `npm run build`

### 2. 碰撞帧 `[PREFILTER]` 调试日志（`8a262ff`）

- **`decision.py` / `log.py`**：`PrefilterDecision` + 终端日志，风格对齐 `[COLLISION][HIT]`
- **仅手腕进框（碰撞帧）输出**，非每帧；速度 tracker 仍每帧 `evaluate`
- 字段：`ankle_max_speed_norm`、`threshold`、`filtered=true/false`；标签 `[FILTERED]` / `[PASS]`
- 开关：`PREFILTER_LOG=1` 或 **`COLLISION_LOG=1`**（compose 已开则自动输出）
- 单测 `tests/test_pick_prefilter_log.py`（10 项全过）

**日志示例**：

```text
[PREFILTER][FILTERED] ... ankle_max_speed_norm=0.194454 threshold=0.08177 filtered=true
[PREFILTER][PASS] ... ankle_max_speed_norm=0.04281 threshold=0.08177 filtered=false
[COLLISION][HIT] ... hits=['shelf1:2']
```

**`ankle_max_speed_norm=—`**：该碰撞帧尚无有效速度（首帧 / gap reset / 踝点 score 不足）；门控 fail-open，通常 `filtered=false`。

### 3. 运行时配置（联调，`8a262ff` 含 `localdata/runtime_config.json`）

| 键 | 值 |
|----|-----|
| `inference.height` | **480** |
| `collision_prefilter.enabled` | **true** |
| `collision_prefilter.speed_threshold` | **0.08177** |

---

## 二、镜像与部署

| 项 | 值 |
|----|-----|
| UI / event-worker 镜像 | `visual-dps-visual-dps-ui:20260629-test-from-4841de6a-ce4fe0a` |
| 本日交付 | **`docker cp` + restart**（`compose up` 会 Recreate 容器，需 **cp 后再启动**） |

**已执行热更新**：

```bash
cd ~/workspace/visual-dps
(cd web && npm run build)
docker cp web/dist/. visual-dps-ui:/app/web/dist/
docker cp services/runtime_config_service.py visual-dps-ui:/app/services/runtime_config_service.py
docker cp services/event_engine/pick_prefilter visual-dps-event-worker:/app/services/event_engine/
docker cp services/event_engine/collision.py visual-dps-event-worker:/app/services/event_engine/collision.py
docker cp services/event_engine/worker.py visual-dps-event-worker:/app/services/event_engine/worker.py
docker cp event_worker.py visual-dps-event-worker:/app/event_worker.py
docker cp services/runtime_config_service.py visual-dps-event-worker:/app/services/runtime_config_service.py
docker compose up -d redis mediamtx visual-dps-ui visual-dps-event-worker
docker restart visual-dps-event-worker
```

**验证**：`PickPrefilterGate` import OK；`test_camera` 联调出现 `[PREFILTER]` + `[COLLISION]`。

---

## 三、本日联调与环境

### 栈

- UI：`http://127.0.0.1:8046`（`.env` `UI_PORT=8046`）
- `visual-dps-event-worker`：`COLLISION_LOG=1`

### test_camera 取流（RTSP 中继）

- 摄像头 **`192.168.1.200:554`** 经网线接开发 PC（以太网 **`192.168.1.100`**，与摄像头小网）
- PC **WLAN `192.168.0.160`** 与 153（`192.168.0.155` / `192.168.1.153`）同局域网
- Windows **portproxy**：`192.168.0.160:8554 → 192.168.1.200:554`（勿绑 `.100`，153 到不了）
- MediaMTX `test_camera` 上游拉 **`192.168.0.160:8554`**（`camera_ips.json` / `mediamtx.yml`，后者 gitignore 由 Dashboard 生成）

### 联调现象

| 现象 | 结论 |
|------|------|
| 153 ping 不通 `192.168.1.100` | 摄像头网与 153 有线网 **物理隔离**；应用 portproxy 到 **PC 的 WiFi IP** |
| `[PREFILTER][FILTERED]` 后无 `[COLLISION][HIT]` | 门控生效，误报被 suppress |
| 偶发 `ankle_max_speed_norm` 偏高（如 0.68） | 查 `pose.ts` 间隔 / 首帧；`—` 为无速度样本 |

---

## 四、问题排查命令

```bash
# 单测
PYTHONPATH=. python -m unittest discover -s tests -v

# 门控 + 碰撞日志
docker logs -f visual-dps-event-worker 2>&1 | rg '\[PREFILTER\]|\[COLLISION\]'
docker logs visual-dps-event-worker 2>&1 | rg 'filtered=true'

# MediaMTX 拉流
docker logs visual-dps-mediamtx 2>&1 | rg 'test_camera|ERR'

curl -s http://127.0.0.1:8046/api/version
```

---

## 五、Git 提交记录（本日）

| Commit | 说明 |
|--------|------|
| `81307a7` | feat: pick_prefilter 接入（ankle_norm + triple90 + shknee140） |
| `007379f` | docs: 2026-07-20 日报（初稿） |
| `8a262ff` | feat: 碰撞帧 [PREFILTER] 调试日志 + runtime 联调配置 |

已 **`git push origin test-from-4841de6a`**。

---

## 六、待跟进

1. **rebuild 镜像** bake 进 `pick_prefilter` + 门控日志（避免 Recreate 丢 cp）。
2. **28 clip replay** 对照 FP/FN；现场观察 FN 是否可接受。
3. **dt 标定**：偶发 norm  spike 时核对 infer `pose.ts` 与 `pose_frame_interval=2`。
4. **监控页 block 原因 overlay**（可选二期）。

---

## 七、相关文档

| 文档 | 说明 |
|------|------|
| [`DAILY-2026-07-02.md`](./DAILY-2026-07-02.md) | pose pacing、ALARM infer 重启 |
| data-collector `prefilter-ankle-norm-speed-threshold.md` | norm 阈值 0.081770 |
| plan `pick_prefilter_接入_d0811afd.plan.md` | 接入计划 |
