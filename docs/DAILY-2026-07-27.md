# 工作日报（2026-07-27）

> 分支 **`test-from-4841de6a`**，本日提交见 **§五**。  
> 主题：**推理 Pipeline 日志统一（feat(log)）** — 全链路 `logging`、轮转、热更新、设置页补全。

---

## 一、背景与目标

07-26 已落地 `pipeline_log` 模块与 `[PIPELINE]` 阶段埋点，但 **infer / event-worker 其余输出仍用 `print`**，文件日志无轮转、配置变更需重启、设置页仅 3 项。本日按方案完成 **统一 logging 中枢** 并修复上述痛点。

---

## 二、架构变更

### 1. `services/pipeline_log.py` 扩展为进程日志中枢

| Logger | 用途 | 输出条件 |
|--------|------|----------|
| `visual_dps.boot` | 启停、delivery 模式 | 始终 stdout |
| `visual_dps.inference` | 模型加载、RTSP、推理会话 | 始终 stdout |
| `visual_dps.pipeline` | `[PIPELINE]` 阶段 trace | `pipeline_log.enabled` + 采样 |
| `visual_dps.collision` | `[COLLISION][HIT/ALARM]` | `COLLISION_LOG` |
| `visual_dps.prefilter` | `[PREFILTER][PASS/FILTERED]` | `PREFILTER_LOG` / `COLLISION_LOG` |
| `visual_dps.callback` | `[CALLBACK][SEND/ACK/...]` | reporting.enabled |

新增 API：`configure_process_logging()`、`reload_process_logging()`、`get_*_logger()`；保留 `log_pipeline_stage` 等兼容接口。

### 2. 文件日志

- `FileHandler` → **`RotatingFileHandler`**
- 配置项：`pipeline_log.max_bytes`（默认 50MB）、`pipeline_log.backup_count`（默认 5）
- 同进程共用一个 `{dir}/{role}.log`（如 `worker.log`、`infer_cam1.log`）

### 3. 热更新

| 进程 | 机制 |
|------|------|
| event-worker | `_refresh_runtime_settings_if_needed` 内调用 `reload_process_logging` |
| infer 容器 | 主循环每 300 帧或 30s 检测 `runtime_config.json` mtime |

**限制**：变更日志目录或轮转参数时打 warning，需 **重启容器** 才切换 file handler；开关/采样可热更新。

---

## 三、代码变更摘要

### print → logging 迁移（推理链路）

| 文件 | 说明 |
|------|------|
| `event_worker.py` | boot logger |
| `services/event_engine/worker.py` | collision / prefilter logger |
| `services/callback_reporter.py` | callback logger |
| `services/inference_service.py` | inference logger + 热更新 |
| `services/rtsp_capture.py` | inference logger |
| `services/inference_backends/*.py` | 模型加载/回退提示 |
| `inference_worker.py` | `configure_process_logging` |

### 配置与 UI

- `runtime_config_service.py`：公开 `max_bytes`、`backup_count`
- `web/src/lib/cameraSettings.js`：补 `stdout`、`dir`、轮转字段与 effectHint；新增 `PIPELINE_LOG_SYSTEM_DEFAULTS`、`formatFieldDefaultValue`、`resolveSettingValue`
- `web/src/pages/SettingsPage.jsx`：支持 text 类型（日志目录）；流水线日志各字段展示 **系统默认值**（布尔开关旁显示开/关，数字/文本带 placeholder）
- `web/src/pages/SettingsPage.css`：`.settings-subsection-lead`、`.settings-field-default` 样式
- `inference_container_service.py`：bind mount 补 `onnx_assets`、`yolo_pose_backend`、`runtime_config_service`
- `README.md`：澄清 pipeline 输出均为 **logging Handler**（stdout / RotatingFileHandler），非 print；补 Handler 对照表与 `file_enabled` 生效条件

### 测试

- `tests/test_pipeline_log.py`：8 项（轮转、boot 隔离、collision 开关、reload、采样等）**全部通过**

---

## 四、部署与验证

本机已执行：

```bash
cd web && npm run build
docker cp web/dist/. visual-dps-ui:/app/web/dist/
docker cp services/*.py … visual-dps-ui / visual-dps-event-worker
docker restart visual-dps-event-worker
```

- event-worker 重启后 boot 日志正常（`Event worker 已启动`、`COLLISION_LOG` 提示等）
- **infer 容器**需各路由 `docker restart visual-dps-infer-*` 加载新代码
- 前端 `npm run build` + `docker cp web/dist` 已同步设置页默认值 UI

### 0720 现场栈核对（只读，未改配置）

| 项 | 结论 |
|----|------|
| UI 挂载 | `visual-dps-0720-deploy/app/localdata`，**非** 开发仓库 `visual-dps/localdata` |
| `app_config.json`（0720） | **无** `pipeline_log` 节 → 不阻塞保存；默认由代码 `_default_pipeline_log_section` 补齐 |
| `runtime_config.json`（0720） | 已有 `pipeline_log.enabled=true`、`sample=1`（设置页保存写入此处） |
| dev 仓库 `runtime_config.json` | **无** `pipeline_log` → 易误判「保存失败」 |
| event-worker | 已有 `pipeline_log.py`；`docker logs` 可见 `[PIPELINE]` worker 三阶段 |
| infer 容器（如 cam1） | **旧镜像**（约 6 月），无 `pipeline_log.py` / `get_pipeline_log_section` → infer 侧无 `[PIPELINE]` |
| 日志文件 | `file_enabled=false`，故无 `localdata/logs/pipeline/` 目录（符合配置） |

---

## 五、Git 提交记录（本日）

| Commit | 说明 |
|--------|------|
| `570569e` | wip: 流水线阶段日志 pipeline_log（config + logging + 设置页） |
| `2dfae69` | **feat(log):** 推理 Pipeline 日志统一 logging + 轮转 + 热更新 |
| `774b992` | **docs:** README 补充 `[PIPELINE]` 字段说明 |
| `2a3a03d` | **feat(ui):** 设置页流水线日志展示系统默认值 + README Handler 说明完善 |

---

## 六、待跟进

1. **0720 推理镜像**：重建或 bind mount 新 `pipeline_log.py` 等，并 restart 各路 infer，才有 infer 侧 `[PIPELINE]`
2. 开启 `pipeline_log.file_enabled` 验证 `{dir}/worker.log`、`infer_camX.log` 轮转落盘
3. 0720 `app_config.json` 可选补 `pipeline_log` 节（与 dev 对齐，非保存必需）
4. dev / 0720 `localdata` 路径统一或文档说明，避免编辑错目录
5. 可选：infer 文件 handler 路径热切换；合入 `pipeline_latency` JSONL

---

## 七、相关文档

| 文档 | 说明 |
|------|------|
| [`DAILY-2026-07-26.md`](./DAILY-2026-07-26.md) | pipeline_log 初版与埋点 |
| [`DAILY-2026-07-20.md`](./DAILY-2026-07-20.md) | event_log 统一字段 |
| [`PIPELINE_SPLIT.md`](./PIPELINE_SPLIT.md) | Redis pose/event 契约 |
