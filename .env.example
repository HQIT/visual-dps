# 复制为 .env 后 docker compose up -d 自动注入
APT_MIRROR=mirrors.aliyun.com
PIP_INDEX=https://pypi.tuna.tsinghua.edu.cn/simple
REDIS_PASSWORD=change-me

# WSL 构建/拉取走 Windows 代理（与 ~/.wsl-proxy.sh 一致）
HTTP_PROXY=http://172.26.128.1:7078
HTTPS_PROXY=http://172.26.128.1:7078

UI_PORT=8045
# 浏览器访问 UI 时用的宿主机 IP/域名（跨机访问必填，勿填 127.0.0.1）
# 本机浏览器访问可填 127.0.0.1；局域网其它电脑访问填服务器 LAN IP，如 192.168.1.153
MEDIAMTX_PUBLIC_HOST=192.168.1.153
MEDIAMTX_RTSP_PORT=8554
MEDIAMTX_HLS_PORT=8888
MEDIAMTX_WEBRTC_PORT=8889
# WebRTC ICE（UDP+TCP 均需映射到 mediamtx 容器 8189）
MEDIAMTX_WEBRTC_ICE_PORT=8189

# RTSP 硬解（仅推理容器；需 inference-lite-gpu 镜像 + Jellyfin FFmpeg + GPU）
RTSP_DECODE_PROFILE=cuda
RTSP_HWACCEL_DEVICE=0
NVIDIA_VISIBLE_DEVICES=all

# 推理镜像（UI 拉起检测容器时使用；WSL 无 GPU 时请保持 0）
INFERENCE_USE_GPU=0
INFERENCE_LITE_GPU_IMAGE=visual-dps-inference-lite-gpu:latest
INFERENCE_LITE_GPU_BASE=visual-dps-inference-lite-gpu-base:latest
INFERENCE_LITE_IMAGE=visual-dps-inference-lite:latest
