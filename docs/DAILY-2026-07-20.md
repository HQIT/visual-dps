# 工作日报（2026-07-20）

> 分支 **`test-from-4841de6a`**，当日 HEAD **`df339d0`**（见 §五）。  
> **镜像 tag**：`20260720-test-from-4841de6a-234a98e`（UI/Event 重编；推理 gpu/gpu-onnx **retag**）。  
> 标定来源：**`visual-dps-data-collector`** `try/speed-filter`（commit **`3793f19`**）。

---

## 一、代码提交（已入库）

### 1. pick_prefilter 模块与 Event Worker 接入（`81307a7`）

**门控规则**（与离线标定一致）：

```text
block = ankle_max_speed_norm > 0.081770
    AND NOT triple90
    AND is_standing(shoulder_hip_knee_angle_min >= 140)
```

- 新增 `services/event_engine/pick_prefilter/`（config / features / angles / gate / service）
- `collision.py` Protocol 钩子；`worker.py` 按 `collision_prefilter.enabled` 注入与 reset
- `runtime_config_service.py`：`collision_prefilter.*` + 设置页「碰撞前置门控」UI

### 2. 碰撞帧 `[PREFILTER]` 调试日志（`8a262ff`）

- **`decision.py`**：`PrefilterDecision`；**仅手腕进框（碰撞帧）输出**
- 开关：`PREFILTER_LOG=1` 或 **`COLLISION_LOG=1`**

### 3. 统一 PREFILTER / COLLISION 终端日志（`234a98e`）

- 新增 **`services/event_engine/event_log.py`**；`worker._log_event_frame` 集中输出
- 每行固定全字段；`[PREFILTER]` 含 **`hits`** 货位 token
- 单测 `tests/test_event_log.py` + `tests/test_pick_prefilter_log.py`

### 4. 分拆 tar 离线部署脚本（`07a8f37`）

- **`scripts/pack-deploy-split-images.sh`**：基于本地镜像打包，**每镜像单独 tar**（非单一 bundle）
- **`scripts/deploy-only/load-split-images.sh`** / **`install-with-images.sh`** / **`verify-package-split.sh`**
- 产出示例：**`/home/hqit/workspace/visual-dps-0720-deploy`**（约 8.7GB）

### 5. 运行时配置（联调）

| 键 | 值 |
|----|-----|
| `inference.height` | **480** |
| `collision_prefilter.enabled` | **true** |
| `collision_prefilter.speed_threshold` | **0.08177** |

---

## 二、镜像与部署

| 项 | 值 |
|----|-----|
| **交付 tag** | `20260720-test-from-4841de6a-234a98e` |
| UI / Event | **`build-ui-image.sh` 重编**（apt 层 CACHED，~12s） |
| 推理 gpu / gpu-onnx | **retag**（自 `20260629-…-ce4fe0a`，未重编 17GB） |
| CPU `inference-lite` | **无本地镜像**；GPU 场景 **`verify-images.sh --skip-lite-cpu`** |

**打 tag 注意**：须 **先改 `.env` 中 `VISUAL_DPS_IMAGE_TAG`** 再跑 `build-ui-image.sh`（脚本内 `source .env` 会覆盖 shell `export`）。

**栈**：`docker compose up -d redis mediamtx visual-dps-ui visual-dps-event-worker`（8046）

---

## 三、离线包 `visual-dps-0720-deploy`

路径：**`/home/hqit/workspace/visual-dps-0720-deploy`**（layout 类似 0529，**分拆 tar**）

| tar | 内容 |
|-----|------|
| `bases-redis-mediamtx.tar` | redis:7 + mediamtx:1.11.3（Docker 29 单独 save mediamtx 仅 8KB，须与 redis 同 tar） |
| `visual-dps-visual-dps-ui--20260720-….tar` | UI |
| `visual-dps-event-worker--20260720-….tar` | Event |
| `visual-dps-inference-lite-gpu--….tar` | GPU 基底 |
| `visual-dps-inference-lite-gpu-onnx--….tar` | ONNX 推理 |

另含 **`app/`**、**`weights/`**、`install.sh`（逐个 `docker load` + compose up）。

**现场安装**：

```bash
cd visual-dps-0720-deploy
./verify-package.sh
# 改 app/.env：REDIS_PASSWORD、MEDIAMTX_PUBLIC_HOST、HOST_PROJECT_ROOT
./install.sh --host <IP> --stop-infer
./verify-images.sh --skip-lite-cpu
```

**0630-deploy `verify-images` 缺 CPU lite**：包内 manifest 未含 `inference-lite`，校验脚本默认仍检查 → 用 **`--skip-lite-cpu`**（与 GPU 栈一致）。

---

## 四、本日联调摘要

| 现象 | 结论 |
|------|------|
| WSL ffmpeg → 153 cam1 `Broken pipe` @ ~20s | MP4 一圈 EOF + RTSP loop；copy 或 shell 重连 |
| `export TAG` 后 build 仍用旧 tag | **`load_build_env` 读 `.env` 覆盖**；先 `sed .env` |
| 0630 缺 `visual-dps-inference-lite` | 从未 build CPU lite；GPU 部署可 skip |

---

## 五、Git 提交记录（本日）

| Commit | 说明 |
|--------|------|
| `81307a7` | feat: pick_prefilter 接入 |
| `8a262ff` | feat: 碰撞帧 [PREFILTER] 调试日志 |
| `234a98e` | feat: 统一 event_log 终端字段 |
| `41199f0` | docs: 日报（event_log） |
| `07a8f37` | feat: 分拆 tar 离线部署脚本 + 日报（0720 镜像/离线包） |
| `df339d0` | docs: 日报更新（0720 镜像/离线包/HEAD） |

已 **`git push origin test-from-4841de6a`**。

---

## 六、待跟进

1. 153 / 现场：`rsync visual-dps-0720-deploy` + `install.sh`
2. **28 clip replay** 对照 FP/FN
3. 可选：`build-ui-image.sh` 外层 `export TAG` 优先于 `.env`（避免再踩坑）
4. 监控页 block 原因 overlay（二期）

---

## 七、相关文档

| 文档 | 说明 |
|------|------|
| [`DAILY-2026-07-02.md`](./DAILY-2026-07-02.md) | pose pacing、ALARM |
| [`BUILD-20260628-test-from-4841de6a-56f47b1.md`](./BUILD-20260628-test-from-4841de6a-56f47b1.md) | 镜像构建记录 |
| data-collector `prefilter-ankle-norm-speed-threshold.md` | norm 0.081770 |
